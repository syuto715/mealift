# v1.5 Phase 2.6 — Completion Notes (Sprint 2.6.3 milestone) 🛡️

Phase 2.6 = ミー先生 chat security hardening. Kickoff anticipated a
greenfield build (6–7 sprints to construct LLMClient + NDJSON + UI +
defenses). Sprint 2.6.1 reconnaissance revealed ~3,573 lines of
production chat infrastructure already deployed and well-architected
for L1/L2/L6 — so the phase collapsed to a focused 3-sprint
gap-close on the missing layers (L3 partial, L4/L5/L7 missing).

## Sprint summary

| Sprint | Output | Drafting cover |
|---|---|---|
| 2.6.1 | Reconnaissance (3,573-line asset inventory) + Drafting 172 7-layer audit + 3-sprint hardening plan + L3 draft + L4/L5/L7 sketches. | 172 audit, 161 boundary clarification |
| 2.6.2 | L3 SYSTEM_PROMPT defense paragraph append (incl. chat-side enhancements: verbatim-repeat refusal + model identity stability) + L4 `MAX_USER_CONTENT_CHARS = 4000` + 6-pattern jailbreak hint logger (EN + JA bilingual, including CJK word-boundary handling). | 172 L3 + L4 |
| 2.6.3 | L5 `scrubSecrets` (3 secret patterns) wired at both chunk emit sites (live stream + idempotency replay) + L7 red-team test suite (7 attack families) + this completion doc. | 172 L5 + L7, 171 reapplication |

## Drafting 172 final matrix — 7 / 7 COMPLETE ✨

| Layer | Implementation | Verified by |
|---|---|---|
| **L1** Architecture (server-only secret) | `coach-chat/index.ts:37` `GEMINI_API_KEY = Deno.env.get(...)`; client bundle confirmed 0 hits for both `AIzaSy*` literal and `GEMINI_API_KEY` symbol. | Sprint 2.6.1 grep audit |
| **L2** Auth (JWT validation) | `coach-chat/index.ts:128-140` Bearer parse + `userClient.auth.getUser()`. | Sprint 2.6.1 read |
| **L3** Defensive system prompt | `coach-chat/index.ts:89-105` appended 7-bullet 「セキュリティと内部情報」 paragraph (covers system-prompt refusal, override defiance, cross-user data refusal, verbatim-repeat refusal, model-identity stability, refusal copy). | Sprint 2.6.2 commit `86bf7a7`; keyword pin in `redTeam.test.ts` |
| **L4** Input sanitization | `security.ts::checkUserContentLength` (4 000 char cap) + `security.ts::detectJailbreakHints` (6 patterns: `ignore_previous_instructions` / `role_override` / `developer_mode` / `system_prompt_extraction` / `role_play_override` / `jailbreak_ja_ignore`). | Sprint 2.6.2; 14 cases in `security.test.ts` |
| **L5** Output filtering | `security.ts::scrubSecrets` (3 secret patterns: `google_api_key` / `openai_anthropic_key` / `jwt`) wired in BOTH `coach-chat/index.ts` chunk emit sites (live stream at line 723 + idempotency replay at line 905). Sentinel `[redacted]` contains no newline so NDJSON line integrity is preserved. `console.warn` telemetry never includes the raw secret. | Sprint 2.6.3; 8 cases in `security.test.ts`; ndjson-integrity case in `redTeam.test.ts` |
| **L6** Context boundary | `_shared/projectContext.ts::projectContextSafeSubset` strict whitelist: `profile.{ageRange, sex, heightCm, weightKg, goalType, activityLevel, trainingDaysPerWeek}` + `targets.{calories, proteinG, fatG, carbG}` + `recentMeals` (top-frequent + aggregates). No PII, no internal IDs, no raw rows. | Sprint 2.6.1 audit; L7 attack-3 pins the architectural property |
| **L7** Red-team tests | `redTeam.test.ts` covers 7 attack families: `disclosure-system-prompt` / `persona-override` / `cross-user-data` / `secret-extraction` / `input-overflow` / `jailbreak-hint-logging` / `ndjson-integrity-on-block`. | Sprint 2.6.3; 13 assertions across the suite |

## Drafting 161 boundary clarification — final verify

| Surface | State |
|---|---|
| `src/stores/chatStore.ts` | untouched |
| `src/infra/repositories/chatRepository.ts` | untouched |
| `src/infra/llm/geminiFlashClient.ts` | untouched |
| `src/infra/llm/ndjsonParser.ts` | untouched |
| `src/infra/llm/coachAdviceClient.ts` | untouched |
| `src/infra/llm/routineGenerationClient.ts` | untouched |
| `src/infra/llm/contextBuilder.ts` | untouched |
| `src/infra/llm/types.ts` | untouched |
| `app/(tabs)/coach/index.tsx` | untouched |
| `app/(tabs)/coach/[id].tsx` | untouched |
| `coach-chat` NDJSON event schema (`meta` / `chunk` / `done` / `error`) | unchanged |
| `coach-chat` JSON error response shape (`{error, message}` + HTTP 400/404/500) | unchanged |
| `coach-chat` request body shape (`{messages, context?, conversationId?}`) | unchanged + new `input_too_long` 400 code which existing clients handle generically |

Phase 2.6 modified `coach-chat/index.ts` (additive: SYSTEM_PROMPT
paragraph, post-parse validators, scrub-on-chunk) + added
`coach-chat/security.ts` + added the two test files. Every change
satisfies the boundary clarification from Sprint 2.6.1: public surfaces
unchanged, internal hardening only.

## Drafting 171 (dev preview hub) reapplication — non-action

Phase 2.4 Sprint 2.4.6 established the dev preview hub at
`/nutrition/v2-hub`. Phase 2.6 *does not* extend it because chat lives
on production routes (`app/(tabs)/coach/*`) and was already
discoverable through the bottom tab. The hub stays semantically
dev-preview-only — same call as Phase 2.5's scan-dish (production
route → no hub entry → purity preserved).

## Test count delta

| Sprint | jest tests | Δ |
|---|---|---|
| Pre-Phase 2.6 | 2350 | — |
| Sprint 2.6.2 (L3 + L4) | 2364 | +14 |
| Sprint 2.6.3 (L5 + L7) | 2385 | +21 |
| **Phase 2.6 total** | — | **+35** |

## SQLite migration head — v38 throughout

Phase 2.6 added zero schema changes. Migration head v38 has now been
stable for **10 consecutive sprints** (2.4.2 through 2.6.3), all of
which honored Drafting 162's "unnecessary migration avoidance"
principle.

## v1.5.x cleanup queue

| Item | Notes |
|---|---|
| Additional secret patterns | `ghp_*` (GitHub PAT), `sk_live_*` (Stripe), specific Supabase key prefixes if any chat-side request surfaces them. Add to `SECRET_PATTERNS` as needed without schema touch. |
| Jailbreak-hint block-mode | Currently log-only (false-positive avoidance). If telemetry from the 6 patterns shows persistent attack volume with low FP rate, escalate selected patterns to soft-block + L3-style refusal copy. |
| Red-team test expansion | Add a new test family per the next chunk of public jailbreak research (Anthropic / Google / OpenAI red-team blog releases). Pattern is established by `redTeam.test.ts`. |
| Live integration tests | The current red-team suite uses pure validators only. A staging deploy verify that calls the actual `coach-chat` EF with each attack and inspects the streamed response copy would complete the L7 picture; defer until the device-verify checkpoint already has a staging environment. |
| Sprint 2.3.7 retrofit (production search.tsx → v2 path) | Independent of Phase 2.6; tracked in Phase 2.4 completion doc. |

## Phase 2.7 hand-off

Phase 2.7 is the v1.5 ship runway:

- **LLMClient back-port** — 5 existing client files
  (`geminiFlashClient` / `coachAdviceClient` / `routineGenerationClient`
  / `contextBuilder` / `types`) currently each implement their own
  fetch + NDJSON parse + error handling. A unified
  `LLMClient` abstraction (the kickoff name) extracts the shared parts
  + per-EF strategies. Phase 2.7 picks this up.
- **Ship docs** — release notes, store-listing copy, the v1.5 changelog
  rolled up from the eight phase-completion docs.
- **Sprint 2.3.7 retrofit window** — production `search.tsx` → v2 path
  (recommended (P2) per `v1.5-phase-2.4-completion.md`), device-verify
  pass on TestFlight, and any hotfixes the verification surfaces.
- **Phase 2.7c — Drafting 143 Exercises master seed (580 entries)** —
  separate strand once the v1.5 chat / nutrition trunk is ship-frozen.

Phase 2.6 closes here. The 3,573 lines of pre-existing chat
infrastructure now have a 7/7 defense-in-depth posture, and the kickoff
prediction that the build cost would be roughly zero held.
