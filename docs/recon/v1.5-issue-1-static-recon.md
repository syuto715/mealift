# v1.5 Issue 1 — 「診断を開始する」 button crash static recon 🔍🛡️

**Sprint**: v1.5 Issue 1 (Build 24 TestFlight dogfood catch)
**Scope**: reconnaissance only — code modification 0 件
**Baseline at recon start**: `git status` clean / `HEAD = 5e57040` (Issue 2 hotfix)
**Codex MCP review**: SKIP (Drafting 175 exception — recon-only)
**Date**: 2026-05-25

---

## 1. Executive summary

「診断を開始する」 button (`app/(tabs)/coach/diagnostic/index.tsx:91-101`) の `onPress` handler は `clearWizard(userId) + router.push('/(tabs)/coach/diagnostic/0')` のみで、 **EF call / async / unhandled promise いずれも literal 不在**。 crash は次画面 `[step].tsx` の mount / 初回 render or Hermes bytecode layer で発生している可能性が高い。

**最有力 root cause hypothesis** (confidence 40%):
**Hermes + New Arch (`newArchEnabled: true`) + expo-router dynamic route `[step].tsx` の production-only bytecode interaction**。 dev build と production-class binary で literal 異なる JS execution layer を通る (Babel/Metro JIT vs Hermes prebuilt bytecode)、 かつ subscriptionService `__DEV__` 早期 return path が dev build で gating logic を mask しているため、 production の `[step].tsx` mount path は **device verify されたことが一度も無い literal 状態**。

**次点 hypothesis**:
- zustand selector `(s) => s.getAnswers(userId)` が空 wizard state で fresh `{}` reference を毎回 return → re-render pressure (confidence 25%)
- RevenueCat init race + `currentTier` module-level state の inter-render mutation (confidence 15%)
- handoff doc 仮説 #4 (Stage 1 expanded PII payload) — code に literal evidence 0、 **当初 hypothesis 反証** (confidence < 5%)

**推奨 next diagnostic step (順番付き)**:
1. **iPhone「解析データ」 toggle ON + 再現** → Apple Crash Logs から literal stack trace を Lane 2 (ASC Crashes) で取得。 これが untouched では他の hypothesis は全て speculative
2. **EAS `preview` profile build (`__DEV__ === false`、 debugger 接続可能)** → Safari Web Inspector で literal reproduce + breakpoint
3. **OTA hotfix 候補**: `[step].tsx` を `<ErrorBoundary>` で wrap + breadcrumb console.log を埋め込んだ JS bundle を `production` channel に push → 次回 crash 時に literal line surface

---

## 2. Task 1-8 evidence sections

### Task 1: 「診断を開始する」 button literal location + handler

**Button**:
```tsx
// app/(tabs)/coach/diagnostic/index.tsx:91-101
<TouchableOpacity
  onPress={handleStart}
  accessibilityRole="button"
  accessibilityLabel="診断を開始する"
  accessibilityHint="7 つの質問にお答えいただきます。 所要時間 約 2 分"
  style={[styles.startButton, { backgroundColor: colors.primary }]}
  testID="diagnostic-start-button"
>
  <Ionicons name="sparkles" size={18} color="#FFFFFF" />
  <Text style={styles.startButtonLabel}>診断を開始する</Text>
</TouchableOpacity>
```

**Handler** (literal verbatim、 line 34-40):
```tsx
const handleStart = () => {
  // Fresh start — wipe any prior incomplete wizard for this
  // user. Cross-account safety already lives in
  // diagnosticStore.reset (called from authStore.logout).
  if (userId) clearWizard(userId);
  router.push('/(tabs)/coach/diagnostic/0');
};
```

**Parent component state** (line 28-32):
```tsx
const profile = useProfileStore((s) => s.profile);
const userId = profile?.id ?? '';
const sub = useSubscription();
const hasAccess = sub.hasFeature('aiCoachGeneration');
const clearWizard = useDiagnosticStore((s) => s.clearWizard);
```

**Visibility gate** (line 80-102): `!hasAccess` で `ProInlineCTA` (Free/locked view)、 hasAccess true なら button render。 Syuto が button を見えた = `hasAccess === true` 確定。

### Task 2: 診断 flow entry point + 起動 sequence

**Sequence after tap**:
1. `handleStart` execute
2. `clearWizard(userId)` — zustand state mutation (`diagnosticStore.ts:77-83` の `delete next[wizardKey(userId)]`)
3. `router.push('/(tabs)/coach/diagnostic/0')` — expo-router push
4. expo-router resolves `/(tabs)/coach/diagnostic/[step].tsx` with `step="0"`
5. `[step].tsx` mounts、 hooks 実行、 initial render

**[step].tsx の mount-time precondition checks** (`app/(tabs)/coach/diagnostic/[step].tsx:39-77`):
- Line 43: `useLocalSearchParams<{ step: string }>()` — defensive
- Line 44: `Math.max(0, Number.parseInt(step ?? '0', 10) || 0)` — defensive parse
- Line 45: `getQuestionByIndex(stepIndex)` — out-of-bounds 時は null return
- Line 47-49: `profile?.id ?? ''` — null-safe
- Line 51-52: `useSubscription()` + `hasFeature('aiCoachGeneration')` — verify Task 5
- Line 54-56: 3 個の zustand selectors — verify Task 7 (selector stability)
- Line 57: `useState(false)` — defensive

**`useEffect` / mount-time async**: `[step].tsx` には **literal 不在** (grep verified、 Task 8 結果に embed)。 final step の `handleAdvance` 内に `submitToGeneration` async 呼出があるが、 これは step=0 mount 時には triggered されない。

### Task 3: coach-routine EF call path

**EF call は initial render path に literal 不在**。

- `src/infra/llm/routineGenerationClient.ts:44-121` `fetchRoutineGeneration` が `${APP_CONFIG.SUPABASE_URL}/functions/v1/coach-routine` を POST するが、 これを呼ぶのは:
- `src/stores/routineGenStore.ts:92` `runGeneration` のみ
- `runGeneration` を呼ぶのは:
  - `src/stores/diagnosticStore.ts:90-106` `submitToGeneration` の **final step 送信時のみ**
  - `src/components/training/RoutineGenerationCard.tsx` の card UI (別 surface)

**Payload 構築** (routineGenerationClient.ts:64-68):
```tsx
body: JSON.stringify({
  intentText: options.intentText,
  exerciseSlugs: options.exerciseSlugs,
  context,
}),
```
- `context = buildUserContext(profileId)` を await — final step まで invoke されない
- `buildUserContext` (`src/infra/llm/contextBuilder.ts:71-76`) は profile null で `throw new Error('[buildUserContext] no profile row found; cannot build context')`
- **first-tap crash の origin としては不適合** — Step 0 mount で context は build されない

**Phase 2.7 7-layer defense との interaction**: Phase 2.7.2/.3 で hardened されたのは **server-side EF (`supabase/functions/_shared/llmSecurity.ts`)** のみ。 client-side `routineGenerationClient.ts` は untouched (Task 6 evidence)。

### Task 4: Stage 1 expanded PII payload (handoff doc 仮説 #4)

```bash
$ grep -rn "expandedPII\|expanded_pii\|PIIPayload\|sanitizePII\|piiPayload" src/ app/
(no matches)

$ grep -rn "Stage 1\|stage1" src/ app/
# 33 hits — all in code comments referring to v1.5 Stage 1 epic、 no expanded-PII pattern
```

`src/infra/llm/contextBuilder.ts:9-14` には PII minimization 記述あり:
```ts
// PII minimization (§6.4):
//   - birth_year is converted to a 5-year AgeRange bucket; the
//     exact value never leaves this layer.
//   - display_name / supabase_uid / email are explicitly NOT read.
//   - Top frequent meal names are bounded at 5 to limit raw
//     food-name exposure at the Gemini boundary.
```

しかし **"expanded PII payload" / "PIIPayload" / "sanitizePII" 等の specific symbol は code に存在しない**。 handoff doc 仮説 #4 は code 上の literal evidence 0 → hypothesis として **反証** (confidence < 5%)。

### Task 5: RevenueCat IAP gating (handoff doc 仮説 #3)

**`subscriptionService.ts:377-385` (hasFeature)**:
```ts
export function hasFeature(
  feature: BooleanFeatureKey,
  status: PlanStatus,
): boolean {
  if (__DEV__) return true;                          // ← 🚨 dev short-circuit
  const required = FEATURE_MATRIX[feature];
  const effective = statusToEffectiveTier(status);
  return TIER_RANK[effective] >= TIER_RANK[required];
}
```

**`subscriptionService.ts:317-320` (getFeatureFlags)**:
```ts
export function getFeatureFlags(): FeatureFlags {
  if (__DEV__) return PLAN_FEATURES.pro;             // ← 🚨 dev short-circuit
  return PLAN_FEATURES[currentTier];
}
```

**precedent commit** (`10614dc fix(subscription): disable DEV_MODE for production builds`、 2026-04-28):
> The MVP-era `const DEV_MODE = true` constant short-circuited getFeatureFlags() and hasFeature() to grant Pro access to every user, which silently disables the entire subscription matrix in production builds. Replaces it with the React Native `__DEV__` global ...

**Implication**:
- dev build (`__DEV__ === true`): hasFeature(anything, anything) → true. すべての user が Pro 扱い、 diagnostic gate 全 open。 → device verify (dev build) で **production gating logic は 1 度も execute されていない literal 状態**
- production build (`__DEV__ === false`): 実際の TIER_RANK 計算 path 起動。 Syuto の TestFlight 用 account が plus/trial/pro なら button visible、 free なら button 不可視

`useSubscription.ts:79-94` で `derivePlanSnapshot(profile)` + `getCurrentTier()` を combine:
```ts
const moduleTier = getCurrentTier();
const tier: PlanTier =
  snapshot.tier === 'plus' && moduleTier === 'pro' ? 'pro' : snapshot.tier;
```
`currentTier` は module-level `let` (subscriptionService.ts:303) — RevenueCat init で `setTier()` 呼出により mutate される (非同期、 timing race の余地)。

`src/infra/services/revenueCatService.ts:96`:
```ts
Purchases.setLogLevel(__DEV__ ? LOG_LEVEL.DEBUG : LOG_LEVEL.WARN);
```
log level のみ (call-path に影響なし)。

### Task 6: Phase 2.7 hardening 範囲 review

```bash
$ git log --since="2026-04-01" --oneline -- \
    "src/**/coach*" "src/**/routine*" "src/**/quiz*" \
    "app/**/coach*" "supabase/functions/coach-routine/**"
96f450b feat(v1.5): Phase 2.7 Sprint 2.7.2 — _shared/llmSecurity.ts lift + wave 1 fan-out (coach-advice + coach-routine) 🛡️
643fd5d feat(v1.5): Phase 1.6 integration polish + UX pass (Codex 5 rounds hardened)
347a750 feat(v1.5): Phase 1.3 diagnostic (② surface) + 1.3.1 doc sync (Codex 7 rounds hardened)
9701653 feat(v1.5): Phase 1.4 + 1.4.1 + 1.5 + 1.5.1 - coach advice ④ + routine generation ③ surfaces (Codex 10 rounds hardened)
3ba93be feat(v1.5): Phase 1.2 chat UI - ミー先生 conversational surface (Codex 3 rounds hardened)
```

**Phase 2.7 client-side touch summary**:
- `96f450b` (Phase 2.7.2): **server-side EF のみ** (`supabase/functions/_shared/llmSecurity.ts` lift + coach-advice / coach-routine fan-out)。 client-side diagnostic / [step] 路径は untouched。
- `643fd5d` (Phase 1.6): `app/(tabs)/coach/diagnostic/[step].tsx` に +18 line。 diff verify (`git show 643fd5d -- "app/(tabs)/coach/diagnostic/[step].tsx"`):
  ```diff
  +            accessibilityHint="コーチタブに戻ります"
  +            accessibilityHint="診断の最初の質問に戻ります"
  +            accessibilityHint="前の質問に戻ります"
  +          accessibilityHint={ isLastStep ? '回答を送信...' : '次の質問へ進みます' }
  +              accessibilityHint={ isSelected ? '選択中の項目です' : 'この項目を選択します' }
  ```
  全 accessibilityHint 追加のみ、 functional change 0。

**Drafting 172 / 173 / 174 適用 commits**:
- いずれも `supabase/functions/_shared/llmSecurity.ts` + 各 EF の server-side。 client-side `[step].tsx` mount path との interaction 0。
- **「診断 起動 sequence」 と interact する Phase 2.7 client commit は literal 0 件**。

### Task 7: production-only / dev-only divergence

**env var 依存** (`src/infra/llm/`、 `src/stores/diagnosticStore.ts`、 `src/stores/routineGenStore.ts`、 `src/infra/services/subscriptionService.ts` で `process.env\.` / `EXPO_PUBLIC_` grep): **0 hits**。 env var 直接 read 0、 全 `APP_CONFIG` 経由。

**`__DEV__` guards** (diagnostic chain で literal 4 件):
| file:line | usage | production effect |
|---|---|---|
| `src/infra/services/subscriptionService.ts:318` | `if (__DEV__) return PLAN_FEATURES.pro;` | dev では gate 全 open、 production では `currentTier` 経由 |
| `src/infra/services/subscriptionService.ts:381` | `if (__DEV__) return true;` (hasFeature) | dev では gate 全 open、 production では TIER_RANK 計算 |
| `src/infra/services/revenueCatService.ts:96` | `setLogLevel(__DEV__ ? DEBUG : WARN)` | log level のみ |
| `src/infra/llm/geminiFlashClient.ts:147` | `if (typeof __DEV__ !== 'undefined' && __DEV__) {` | debug log block (call-path 影響なし) |

**asset bundling**: search-index.json 同等の "missing asset → production crash" pattern は diagnostic path に literal 不在。 diagnostic は `DIAGNOSTIC_QUESTIONS` 定数 (`src/domain/diagnosticQuestions.ts:9-94`) のみ参照、 JSON / image / external asset 0。

**Hermes / New Arch interaction**:
- `app.config.ts:14`: `newArchEnabled: true`
- New Architecture (Fabric + TurboModules) は production class build で activate、 dev build (Expo Go) でも有効だが optimization layer 異なる
- `[step].tsx` は dynamic route (file name `[step].tsx`)、 expo-router で URL param 経由 mount される
- **production-only crash の最有力 candidate**: New Arch + Hermes prebuilt bytecode + dynamic route の interaction で `[step].tsx` mount が失敗

### Task 8: 過去 cleanup queue / TODO review

```bash
$ grep -rn "TODO.*診断\|FIXME.*diagnos\|FIXME.*routine\|TODO.*coach-routine" src/ app/
(no matches)
```

Phase 2.7 completion doc (`docs/plans/v1.5-phase-2.7-completion.md`) の cleanup queue:
- Sprint 2.7.3 carryover: L4 length cap boundary 試験 + multi-pattern jailbreak overlap 試験 (server-side LLM hardening)
- Sprint 2.7.5 4 round carryover: ship docs polish
- **「診断」 / "diagnostic" / "coach-routine client" 関連 TODO は cleanup queue に literal 不在**

完了 doc (`docs/plans/v1.5-phase-2.7-completion.md`) 内 grep `diagnos|routine`:
- 全 hits は **server-side EF + Drafting 173 fan-out** 文脈
- client-side diagnostic UI に関する deferred item 0

---

## 3. Candidate root cause matrix

| # | Hypothesis | Evidence (file:line) | Confidence | Fix difficulty | Verify path |
|---|---|---|---|---|---|
| H1 | **Hermes / New Arch + expo-router dynamic route の production-only mount failure** | `app.config.ts:14` (`newArchEnabled: true`)、 `app/(tabs)/coach/diagnostic/[step].tsx` is dynamic route file、 dev build では Babel/Metro JIT + Hermes RAM bundle、 production では Hermes prebuilt bytecode | **40%** | 軽-中 (回避策: dynamic route を fixed `[step0].tsx`〜`[step6].tsx` に展開 / `newArchEnabled: false` で再 build / ErrorBoundary 追加) | EAS `preview` profile (production-class、 debugger 接続可) で reproduce → Safari Web Inspector で literal stack trace |
| H2 | **zustand selector `(s) => s.getAnswers(userId)` が空 wizard で fresh `{}` を毎 invocation で return → production Hermes で re-render escalation** | `[step].tsx:54`: `useDiagnosticStore((s) => s.getAnswers(userId))`、 `diagnosticStore.ts:62`: `get().wizards[wizardKey(userId)]?.answers ?? {}` ← `??` で fresh literal `{}` を毎回構築 | **25%** | 軽 (3-5 line fix: selector を `wizards[userId]?.answers` に変えて `useMemo` で empty case を stable empty ref に) | dev build で selector を意図的に unstable 化 + React.StrictMode wrap で reproduce、 or production `<ErrorBoundary>` で "Maximum update depth exceeded" catch |
| H3 | **RevenueCat init race + `currentTier` module-level state inter-render mutation** | `subscriptionService.ts:303` `let currentTier: PlanTier = 'free'`、 `useSubscription.ts:83-87` で moduleTier 経由 upgrade、 entry → [step] 遷移中に `setTier()` 非同期 invoke の余地 | **15%** | 中 (currentTier を zustand store 化 + ReanimatedValue 化 + applyCustomerInfoToProfile の atomic 化) | RevenueCat init timing を console.log で trace + 連続 5-10 回 button tap で race rate 測定 |
| H4 | **subscriptionService `derivePlanSnapshot` の `Date.parse(planExpiresAt)` で NaN propagation** | `subscriptionService.ts:433` `Date.parse(planExpiresAt) > now.getTime()` — malformed string で NaN return、 `NaN > number === false` で safe path だが、 後続 計算で NaN propagate possibility | **10%** | 軽 (1 line: `Number.isFinite(Date.parse(planExpiresAt))` guard) | Syuto の profile.planExpiresAt 値 を Supabase Studio で直接 read |
| H5 | **handoff doc 仮説 #4 (Stage 1 expanded PII payload)** | grep `expandedPII\|expanded_pii\|PIIPayload\|sanitizePII` → 0 hits、 contextBuilder.ts は PII minimization のみ | **< 5%** | n/a | 反証済 — 次 hypothesis ハント不要 |

---

## 4. Recommended hotfix scope

**確信度 60% 以上の hypothesis 不在 → additional verify が必要**:

### Step A (immediate、 user-driven、 0 code change):
1. **iPhone「解析データ」 toggle ON**: 設定 → プライバシー → 解析と改善 → 「分析を共有」 ON。 翌日以降 ASC (App Store Connect) Crashes lane に literal stack trace surface。 **これが root cause 確定の SSoT path**。
2. 「診断を開始する」 tap 再現 3-5 回 (crash 再現性確認 + log 蓄積)。

### Step B (agent-driven、 1-2 hour 軽 OTA 候補):
1. **`<ErrorBoundary>` wrap**: `app/(tabs)/coach/diagnostic/_layout.tsx` の `<Stack>` 内側を `ErrorBoundary` (new file `src/components/shared/ErrorBoundary.tsx`) で包む。 production crash 時に Toast + Sentry-like error surface。 single-file diff + jest test + OTA push 可。
2. **selector stability fix (H2 defensive)**: `[step].tsx:54` を:
   ```tsx
   // 現在 (unstable):
   const answers = useDiagnosticStore((s) => s.getAnswers(userId));
   // 修正案 (stable):
   const wizard = useDiagnosticStore((s) => s.wizards[userId]);
   const answers = useMemo(() => wizard?.answers ?? EMPTY_ANSWERS, [wizard]);
   // module-top: const EMPTY_ANSWERS: DiagnosticAnswers = {};
   ```
   regression risk: 低 (semantics 同一、 reference stability 改善)。

### Step C (build-side、 半-1 day):
1. **EAS preview profile build**: `eas build --profile preview --platform ios` で `__DEV__ === false` + debuggable binary を生成。 Syuto iPhone へ TestFlight 経由 install → Safari Web Inspector で `[step].tsx` mount を breakpoint。 H1 / H2 / H3 を literal verify 可能。
2. **`newArchEnabled: false` regression test build** (H1 isolation): 一時的に `app.config.ts:14` を false に変えて preview build → 同 user で再現 → 再現すれば H1 反証、 再現しなければ H1 強い証拠。

### NO-CODE 推奨 (recon-only sprint scope):
今 sprint では **defensive fix は coding せず**、 上記 Step A の crash log lane を Syuto 側で activate するのが最も cost-effective。 Step B の defensive coding は別 sprint (Issue 1 hotfix sprint) で Drafting 175 + 177 適用しつつ実装。

---

## 5. Risk flags

🚨 **Production-only path**:
- `__DEV__` short-circuit (subscriptionService.ts:318, :381) により **production gating logic は 1 度も device verify されていない literal 状態**。 これ自体は crash 原因ではないが、 production path の "first contact" であり、 周辺 race condition / null safety bug を mask している可能性大。

🚨 **dev build で reproduce 不可能性**:
- `__DEV__ === true` で hasFeature 全 true 化 → dev build の Syuto account は常に「Pro」 扱い → production の "現実の plan tier" 経由 path は dev で execute されない。 dev reproduce には **`__DEV__` を意図的に false 化** が必要 (script で global override or preview profile build)。

🚨 **Phase 2.7 hardening との interaction risk**:
- Phase 2.7.2/.3 は **server-side EF のみ** touch。 client-side coach-routine path は untouched (Task 6)。 ただし `_shared/llmSecurity.ts` lift で server response 形状が変化した場合、 `routineGenerationClient.ts:106-119` の response validation が新たな error 投げる可能性 — **final step submit 時のみ顕在化**、 first-tap crash 範囲外。

🚨 **New Arch (`newArchEnabled: true`) の literal リスク**:
- Expo SDK 54 + new architecture は 2026 年時点で stable だが、 サードパーティ native module の互換性は要 verify。 `@kingstinct/react-native-healthkit`、 `@react-native-ml-kit/text-recognition`、 RevenueCat 等 が New Arch で fully production-tested かは Phase B device verify で初めて検出される。

🚨 **OTA hotfix の射程限界**:
- ErrorBoundary 追加 / selector stability fix は JS bundle 変更 → EAS Update OTA で push 可。 ただし H1 (New Arch + Hermes bytecode interaction) なら native code レベルの問題 → OTA で fix 不可能、 Build 25 native binary が必須。

---

## 6. chat-side relay summary

**Issue 1 「診断を開始する」 button crash — 静的 recon 結果**:

- **最有力 root cause** (40%): Hermes + New Arch (`newArchEnabled: true`) + expo-router dynamic route `[step].tsx` の production-only mount failure。 dev build では `__DEV__` 早期 return が gating logic を mask、 かつ Babel/Metro JIT で bytecode 異なる → production path は 1 度も device verify されたことが無い literal 状態。
- **次点** (25%): zustand selector `getAnswers(userId)` が空 wizard で fresh `{}` 毎回 return → 再 render escalation。
- **反証** (< 5%): handoff doc 仮説 #4 (Stage 1 expanded PII payload) — code に literal 0 hits。

**確信度 60% 以上の hypothesis 不在** → defensive coding fix を今 sprint で着手するより、 **Lane 2 (Apple Crash Logs) を Syuto iPhone「解析データ」 toggle ON で activate** が SSoT path。 並行して agent-side で `<ErrorBoundary>` + selector stability fix を OTA hotfix sprint で landing 可 (1-2 hour、 single-file change + jest)。

**Recommended next sprint**: Issue 1 hotfix sprint、 estimated 1.5-3 hour、 file 範囲は `app/(tabs)/coach/diagnostic/[step].tsx` + `src/components/shared/ErrorBoundary.tsx` (new) + `src/stores/diagnosticStore.ts` selector stability。 Drafting 175 + 177 適用、 stack trace surface 後に re-prioritize (H1 native fix が必要なら Build 25 binary 必須、 H2/H3 なら OTA で完結)。

---

🔍🛡️🌱
