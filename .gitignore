# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
# Expo
.expo/
dist/
web-build/
expo-env.d.ts
# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
# Metro
.metro-health-check*
# debug
npm-debug.*
yarn-debug.*
yarn-error.*
# macOS
.DS_Store
*.pem
# local env files
.env
.env*.local
# typescript
*.tsbuildinfo
# generated native folders
/ios
/android
# MEXT food composition table — raw download cache (not tracked; re-run importer to fetch)
scripts/import-mext-foods/downloaded/
.env.save
# Supabase CLI cache (regenerated on every `supabase functions deploy` / link).
# Tracking these makes the working tree dirty after every CLI invocation.
supabase/.temp/
*.swp
